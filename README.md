# multi_agentic_research_using_langchain

Environment setup

conda create -n langchain_multi_agent python=3.11 -y (using conda)
Using uv:
1. uv init 
2. uv venv
3. write in requirements.txt and run uv add -r requirements.txt
4. .venv\Scripts\activate (for uv)